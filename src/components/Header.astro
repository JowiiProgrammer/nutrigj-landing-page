---

---

<header class="relative min-h-screen flex items-center justify-center overflow-hidden bg-gradient-to-br from-nutrigj-black via-nutrigj-black to-nutrigj-primary/20">
  <!-- Background and Hero Content -->
  <div class="absolute inset-0 z-0">
    <div class="absolute inset-0 bg-black/50"></div>
    <img
      src="/images/header-bg.webp"
      alt="Deportista en entrenamiento"
      class="w-full h-full object-cover"
    />
  </div>

  <div class="container mx-auto px-4 z-10 text-center">
    <div class="w-64 md:w-80 mx-auto mb-8 animate-fade-in">
      <img
        src="/images/logo-w.webp"
        alt="NutriGJ Logo"
        class="w-full h-auto"
      />
    </div>
    <h1 class="text-4xl md:text-6xl font-bold text-white mb-6 animate-slide-up">
      Transforma tu rendimiento con una
      <span class="text-nutrigj-primary">nutrición personalizada</span>
    </h1>
    <p class="text-xl text-gray-200 mb-8 max-w-2xl mx-auto animate-slide-up-delayed">
      Alcanza tus metas deportivas con planes nutricionales diseñados específicamente para ti
    </p>
    <div class="flex flex-col sm:flex-row gap-4 justify-center animate-fade-in-delayed">
      <a
        href="#contacto"
        class="bg-nutrigj-primary hover:bg-nutrigj-primary/90 text-white px-8 py-3 rounded-full font-semibold transition-all transform hover:scale-105"
      >
        Agenda tu cita
      </a>
      <a
        href="#servicios"
        class="bg-transparent border-2 border-white hover:border-nutrigj-primary text-white px-8 py-3 rounded-full font-semibold transition-all transform hover:scale-105"
      >
        Descubre más
      </a>
    </div>
  </div>
</header>


<style>
  .animate-fade-in {
    animation: fadeIn 1s ease-out;
  }
  
  .animate-slide-up {
    animation: slideUp 1s ease-out;
  }
  
  .animate-slide-up-delayed {
    animation: slideUp 1s ease-out 0.2s backwards;
  }
  
  .animate-fade-in-delayed {
    animation: fadeIn 1s ease-out 0.4s backwards;
  }
  
  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }
  
  @keyframes slideUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>